# 1.첫번쨰 값을 입력받습니다.
# 2.연산자를 입력받습니다.
# 3.피연산자를 입력받습니다.
# 4.값을 출력합니다

a = int(input('연산할 값 : ')) # 연산할 값을 출력하는 함수
b = input() # 연산자를 출력하는 함수
c = int(input("input second number : ")) # 두번쨰 연산할 값을 출력하는 함수

if b == "+" : # 만약 b와+ 같으면
 print(a+c) # a+c를 출력
elif c == "-" : #만약 c와- 같으면
  print(a-c) # a-c를 출력
elif c == "*" : # c와* 같으면
 print(a*c) # a*c를 출력
elif c == "/" : # 만약 c와/ 같으면
  print(a/c) # a/c를 출력 
else : #다르면
  print('연산자 입력오류') #연산자 입력오류 출력
